import React from 'react'
import { View, Text } from 'react-native'

type Props = {}

export default function Settings({}: Props) {
    return (
        <View>
            <Text>Settings</Text>
        </View>
      )
}